{
  "name": "@thullo/root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "jest --testPathIgnorePatterns=/scripts/templates/usecases/",
    "dev:front": "npm run start --prefix packages/front",
    "dev:api": "npm run dev --prefix packages/express"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "lerna": "^4.0.0",
    "ts-jest": "^27.0.7"
  }
}
