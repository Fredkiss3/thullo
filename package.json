{
  "name": "@thullo/root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "lerna bootstrap",
    "test": "jest --testPathIgnorePatterns=/scripts/templates/usecases/",
    "dev:front": "npm run start --prefix packages/front",
    "dev:api": "npm run start --prefix packages/express",
    "dev": "npm-run-all --parallel dev:front dev:api"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "ts-jest": "^27.0.7"
  }
}
