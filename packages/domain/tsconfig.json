{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist",
        "rootDir": "./src"
    },
    "include": ["**/*.ts"],
    "exclude": ["node_modules", "tests", "scripts", "uml", "dist"]
}
